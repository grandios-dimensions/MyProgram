/** * 「アルゴリズムとデータ構造」 * Hirohito Shibata (hshibata@edu.teu.ac.jp) * * 「選択ソート」 * 使い方:  *   - SelectSort.sort(D)				配列 D をソート *   - SelectSort.sort(D, left, right)	配列 D の left...right の範囲をソート */public class SelectSort extends SuperSort {    // 配列 a を「選択ソート」でソートする。    public static void sort(int[] a) {	for (int i = 0; i < a.length - 1; i++) {	    // 最小の要素を探す	    int min_index = i;	// 最小の要素のインデックス	    for (int j = i + 1; j < a.length; j++) {		if (a[j] < a[min_index]) {		    min_index = j;		}	    }	    // i番目の要素を最小のものに入れ替え	    swap(a, i, min_index);	    if (process || detail) {	// 途中経過を表示		System.out.println(get_string(a, i));	    }	}    }}