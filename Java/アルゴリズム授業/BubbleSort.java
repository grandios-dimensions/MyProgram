/** * 「アルゴリズムとデータ構造」 * Hirohito Shibata (hshibata@edu.teu.ac.jp) * * 「バブルソート」 * 使い方:  *   - BubbleSort.sort(D)				配列 D をソート */public class BubbleSort extends SuperSort {    // 配列 a を「バブルソート」でソートする。    public static void sort(int[] a) {	for (int i = a.length - 1; i > 0; i--) {            for (int j = 1; j <= i; j++) {                if (a[j-1] > a[j])                    swap(a, j-1, j);            }            // 途中経過を表示	    if (process || detail) 			System.out.println(get_string(a, i));        }    }}