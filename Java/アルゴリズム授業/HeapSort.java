/** * 「アルゴリズムとデータ構造」 * Hirohito Shibata (hshibata@edu.teu.ac.jp) * * 「ヒープソート」 * 使い方:  *   - HeapSort.sort(D)				配列 D をソート */public class HeapSort extends SuperSort {    // 配列 a を「選択ソート」でソートする。    public static void sort(int[] a) {	// ヒープを生成して初期化	Heap heap = new Heap();	heap.initialize(a.length);	// 全ての値をヒープに格納	for (int i = 0; i < a.length; i++) {	    heap.push_heap(a[i]);	}	// ヒープから1つずつ取り出して、大きい順に後ろから並べる	for (int i = a.length - 1; i >= 0; i--) {	    a[i] = heap.delete_maximum();	    // 途中経過を表示	    if (process || detail) {		System.out.println(get_string(a, i));	    }	}    }}